## -*- mode: makefile; -*-

PKG_CPPFLAGS = -O3 -I. -DNDEBUG
PKG_LIBS = $(LAPACK_LIBS) $(BLAS_LIBS) $(FLIBS)

os    := $(shell uname -s)
ifeq ($(os),Linux)
	PKG_CXXFLAGS += -DEIGEN_USE_BLAS
endif

.PHONY: all clean

all: $(SHLIB)

clean:
	$(RM) *.o
	$(RM) *.dll
	$(RM) *.so
	$(RM) *.dylib
